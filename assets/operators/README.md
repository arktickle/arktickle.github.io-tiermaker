# 干员图片目录说明

当前仅保留一个目录：

- `assets/operators/all/`

将头像文件放入该目录后，可运行数据重建脚本（由我已执行）生成 `data/operators.data.js`。

命名规则：

- 普通文件名：`角色名.png` / `角色名.webp`
- 若文件名为 `头像_角色名.xxx`，页面会自动忽略 `头像_` 前缀，显示为 `角色名`

页面中头像会统一为固定尺寸圆形显示（列表与坐标图内都统一），不同原图尺寸/格式（png、webp 等）会自动裁切适配。
